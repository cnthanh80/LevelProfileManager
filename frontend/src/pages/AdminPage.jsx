import { Card, Col, Row, Table } from 'antd';
import { useEffect, useState } from 'react';
import PageHeader from '../components/PageHeader';
import JsonView from '../components/JsonView';
import StatusTag from '../components/StatusTag';
import { api } from '../api/client';
export default function AdminPage(){ const [data,setData]=useState({}); const load=async()=>{ const calls=[api.users({limit:100}),api.organizations({limit:100}),api.roles(),api.identityProviderStatus(),api.securitySummary(),api.securityEvents(),api.runtime(),api.productionChecklist()]; const r=await Promise.allSettled(calls); const val=(i,d)=>r[i].status==='fulfilled'?r[i].value:d; setData({users:val(0,{items:[]}),orgs:val(1,{items:[]}),roles:val(2,[]),idp:val(3,{}),sec:val(4,{}),events:val(5,[]),runtime:val(6,{}),prod:val(7,{})}); }; useEffect(()=>{load();},[]); return <><PageHeader title="Quản trị hệ thống" subtitle="Người dùng, vai trò, tổ chức, LDAP/SSO và trạng thái hardening." /><Row gutter={[16,16]}><Col span={12}><JsonView title="Identity Provider" data={data.idp}/></Col><Col span={12}><JsonView title="Security Summary" data={data.sec}/></Col><Col span={12}><JsonView title="Runtime" data={data.runtime}/></Col><Col span={12}><JsonView title="Production Checklist" data={data.prod}/></Col></Row><Card title="Người dùng" style={{marginTop:16}}><Table rowKey="id" dataSource={data.users?.items||[]} columns={[{title:'Username',dataIndex:'username'},{title:'Email',dataIndex:'email'},{title:'Full name',dataIndex:'full_name'},{title:'Active',dataIndex:'is_active',render:v=><StatusTag value={v?'active':'inactive'}/>},{title:'Provider',dataIndex:'auth_provider'}]} /></Card><Card title="Tổ chức" style={{marginTop:16}}><Table rowKey="id" dataSource={data.orgs?.items||[]} columns={[{title:'Code',dataIndex:'code'},{title:'Name',dataIndex:'name'},{title:'Type',dataIndex:'org_type'}]} /></Card><Card title="Security events" style={{marginTop:16}}><Table rowKey="id" dataSource={data.events||[]} columns={[{title:'Time',dataIndex:'created_at'},{title:'Type',dataIndex:'event_type'},{title:'Severity',dataIndex:'severity'},{title:'Detail',dataIndex:'detail'}]} /></Card></>;}
